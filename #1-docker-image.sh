# See docker list
docker image ls

# Download docker image
# Format -> docker image pull <imageName>:tags
docker image pull redis:latest

# Hapus/Remove Docker Image
# Format -> docker image rm <imageName>:tags
docker image rm redis:latest
